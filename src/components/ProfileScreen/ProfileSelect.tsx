import React from "react";
import { Text, View } from "react-native";

const ProfileSelect = () => {
    return (
        <View>
            <Text>Hola</Text>
        </View>
    );
};

export default ProfileSelect;
